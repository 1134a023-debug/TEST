HW0415-製作拆炸彈終極密碼裝置

• 使用UI的物件,Button、Text、Image,實作終極密碼
• Scene物件
• 規劃3個Scene物件,分別為載入計算機Scene(可以加上Sprite影格動畫
效果)、計算機UI Scene、結束計算機Scene(可以加上Sprite影格動畫)。
• Text物件
• 用於密碼次數、密碼結果顯示、按鈕按壓的互動內容顯示
• Button物件
• 作為數字的輸入:0~9
• 功能操作:如密碼輸入、提示...功能操作與Clear清除功能。
• Image物件
• 製作過關圖形動畫或是失敗動畫(超過N次錯誤)。
• 繳交方式
1. 提供程式碼CS(C#)檔案。
2. 提供專案檔案壓縮檔。
3. 提供Game視窗的結果錄影檔。這是這次的作業
作業更新
題目:多重場景專案檔
• 專案檔下載點參考課程資訊平台
• 說明:
1. GameScene新增TextField 供玩家輸入名稱,並顯示。
2. 試著利用Random.Range() 亂數函數,變更星星的大小,每次出
現星星大小不一樣
3. 增加切換Scence程式碼,對應的按鈕按下後可以切換Scene(前往
Step 4)。
4. 切換至 UI 終極密碼遊戲,帶入玩家輸入名稱並顯示,對應的按鈕
按下後可以切換Scene回到前面的Scene(回到Step 2)。

• 繳交方式
1. 專案檔:提供專案檔案壓縮檔。
2. 成果分享:提供Game視窗的結果錄影檔、提供程式碼(C#)檔案。
這個作業我想看能不能在用上次做的做小更新
