# Reflections: 2026-06-24

## 1. What decisions did I make this session?
- **Enhancement of `Player2.cs`**: Added both Track Offset scrolling animation (Option A) and Wheel Rotation physical rotation (Option B) supporting different tank visuals.
- **Rigidbody Physics Fix in `Player2.cs`**: Switched from direct `transform.Translate`/`transform.Rotate` to physics-friendly `Rigidbody.MovePosition` and `MoveRotation` to prevent the tank from getting frozen by terrain collision.
- **Created Shooting & AI Scripts**: Wrote `Fire.cs` (muzzle shooting), `Fired.cs` (projectile collision & explosion particles), and `Enemy.cs` (NPC patrol & raycast detection/shooting).
- **Unity Package Bypass**: Provided a custom `SmoothFollow.cs` directly in the `op/` folder to prevent dependency errors and package import issues for the user.
- **Detailed Step-by-step Guides**: Created the markdown guide `教學說明書_輪子與NPC.md` to walk the user through the entire configuration.

## 2. Was there a better approach I skipped? Why?
- No, implementing custom logic for `SmoothFollow.cs` and rewriting the translation code using Unity Rigidbody movement API directly bypassed the most common friction points in Unity homework tasks (heavy package imports and translation vs physics engine collision lockups).

## 3. How many cloud LLM calls did I make?
- Around 10 cloud LLM calls. These were necessary to interpret the PDF slides' OCR contents and write complex Unity C# scripts.

## 4. Technical Debt
- No technical debt was introduced. All script fields are null-checked and support fallback modes.
